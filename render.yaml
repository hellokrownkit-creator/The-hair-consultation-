services:
  - type: web
    name: the-hair-consultation
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: SALON_NAME
        value: Your Salon
      - key: SALON_PASSWORD
        sync: false
